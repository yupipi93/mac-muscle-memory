# My keyboard

Model, how it connects, and its USB ids (`ioreg -p IOUSB -l -w 0`).

## Remaps stored on the keyboard

Firmware remaps are invisible to every probe in this repo. Record each one when you make it.

| Layer | Physical key | Default | Sends now | Why |
|-------|--------------|---------|-----------|-----|
| | | | | |
